refresh_rate: 30 seconds

appenders:
  stdout:
    kind: console
    encoder:
      pattern: "{l} {d(%Y-%m-%d %H:%M:%S%.3f)} [{M} - {L}] - {m}{n}"

root:
  level: info
  appenders:
    - stdout

loggers:
  main:
    level: debug
  rest:
    level: debug
  dao:
    level: debug


